{
  "name": "crowdwave-stripe-backend",
  "version": "1.0.0",
  "description": "Simple Stripe backend for CrowdWave payments",
  "main": "stripe_backend.js",
  "scripts": {
    "start": "node stripe_backend.js",
    "dev": "nodemon stripe_backend.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "stripe": "^13.0.0",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
